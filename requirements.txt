anthropic>=0.40.0
httpx>=0.27.0
langgraph>=1.2.0
# Durable intercom state: without a real checkpointer, a backend restart loses
# every in-flight resident conversation. Needs psycopg 3 (SQLAlchemy stays on
# psycopg2 below; the two coexist).
langgraph-checkpoint-postgres>=3.1.0
psycopg[binary,pool]>=3.2.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
SQLAlchemy>=2.0.0
bcrypt>=4.1.0
PyJWT>=2.8.0
email-validator>=2.1.0
pytest>=8.0.0
