import { ui } from '../ui'

// Scaffold stub — implemented by E5 (layout view). Replace this whole file.
export default function Layout() {
  return (
    <div style={ui.page}>
      <h1 className="page-title">Layout</h1>
      <div style={ui.sub}>Coming soon.</div>
    </div>
  )
}
