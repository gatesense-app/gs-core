import { ui } from '../ui'

// Scaffold stub — implemented by E6-S1/S2 (flat detail + manage residents).
// Replace this whole file. Route is /flat/:id; read the id with useParams().
export default function FlatDetail() {
  return (
    <div style={ui.page}>
      <h1 className="page-title">Flat</h1>
      <div style={ui.sub}>Coming soon.</div>
    </div>
  )
}
